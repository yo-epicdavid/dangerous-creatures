---
import { asset, lp } from '../lib/data.js';
const { id, name, thumb, locale = 'en' } = Astro.props;
---
<a class="creature" href={lp(locale, `/animal/${id}`)}>
  <img src={asset(thumb)} alt={name} loading="lazy" />
  <span>{name}</span>
</a>
