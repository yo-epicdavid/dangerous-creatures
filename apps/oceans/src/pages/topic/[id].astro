---
import TopicView from '../../components/TopicView.astro';
import { listEntries } from '../../lib/data.js';

export function getStaticPaths() {
  return listEntries('en').map((d) => ({ params: { id: d.id }, props: { d } }));
}
const { d } = Astro.props;
---
<TopicView d={d} locale="en" />
