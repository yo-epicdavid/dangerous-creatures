---
import DinoView from '../../components/DinoView.astro';
import { listEntries } from '../../lib/data.js';

export function getStaticPaths() {
  return listEntries('en').map((d) => ({ params: { id: d.id }, props: { d } }));
}
const { d } = Astro.props;
---
<DinoView d={d} locale="en" />
