packages:
  - 'apps/*'
  - 'packages/*'

# allow the native postinstall build scripts the Astro/Vite toolchain needs
allowBuilds:
  esbuild: true
  sharp: true

minimumReleaseAgeExclude:
  - astro@7.0.4
