{
  "name": "ms-exploration-archive",
  "private": true,
  "version": "1.0.0",
  "description": "Modern, museum-style web rebuilds of Microsoft Home's Exploration Series CD-ROMs (Dangerous Creatures, Oceans, Dinosaurs).",
  "scripts": {
    "build": "pnpm -r build",
    "dc:dev": "pnpm --filter dangerous-creatures dev",
    "dc:build": "pnpm --filter dangerous-creatures build",
    "oceans:dev": "pnpm --filter oceans dev",
    "oceans:build": "pnpm --filter oceans build"
  }
}
